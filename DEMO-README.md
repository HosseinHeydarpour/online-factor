# 🔒 راهنمای حالت دمو ضدریست (Tamper-Proof Demo Mode)

## نحوه تولید لینک دمو برای مشتری

### مرحله ۱ — صدور لینک اختصاصی با انقضای قطعی

شما می‌توانید برای هر مشتری یک لینک با مدت دلخواه (مثلاً ۲ روز یا چند ساعت) بسازید:

```bash
# ۲ روز دمو (پیش‌فرض):
node scripts/get-demo-token.cjs "https://your-site.com/" 2d

# یا برای تست لوکال (مثلاً ۱۰ ثانیه یا ۲ دقیقه):
node scripts/get-demo-token.cjs "http://localhost:3000/" 10s
node scripts/get-demo-token.cjs "http://localhost:3000/" 2m
```

خروجی نمونه:
```
🔑 لینک دمو اختصاصی تولید شد (با انقضای قطعی و ضدریست):
────────────────────────────────────────────────────────────
⏱️  مدت اعتبار:   2d
🔐 توکن امضاشده: mujqwik3--23ff977a
⏰ موعد انقضا:   ۱۴۰۵ مهر ۵, یکشنبه ساعت ۱۵:۰۸:۴۴
🔗 لینک مشتری:   https://your-site.com/?demo=mujqwik3--23ff977a
────────────────────────────────────────────────────────────
```

### مرحله ۲ — ارسال لینک به مشتری

همین لینک را به مشتری بدهید. مشتری تا زمان مشخص‌شده دسترسی دارد.

---

## 🛡️ چرا مشتری نمی‌تواند دمو را ریست کند؟

۱. **تاریخ انقضا در خودِ لینک تعبیه و امضا شده است:**  
   توکن از دو بخش `[تاریخ انقضا] - [امضای دیجیتال با کلید مخفی]` تشکیل شده است.  
   پس از پایان موعد انقضا، آن لینک **برای همیشه باطل می‌شود**؛ یعنی حتی اگر مشتری:
   - لینک را دوباره باز کند
   - کش و حافظه مرورگر را به طور کامل پاک کند (Clear site data)
   - از مرورگر دیگری (کروم، فایرفاکس، اج) استفاده کند
   - یا حتی با کامپیوتر یا گوشی دیگری لینک را باز کند  
   سامانه زمان انقضای موجود در خود لینک را با ساعت واقعی می‌سنجد و چون موعد گذشته است، **اجازه ورود نخواهد داد و مستقیماً صفحه قفل را نشان می‌دهد**.

۲. **سنگ‌قبر چندلایه (Permanent Poison Pill):**  
   هنگام انقضا، کلیه داده‌های حساس کاربری (فاکتورها، نرخ‌ها، حساب‌ها، مشتریان) پاک می‌شوند، اما یک مُهر قفل دائمی رمزنگاری‌شده در Cookie و localStorage کاشته می‌شود تا روی همان مرورگر قفل بماند.

۳. **مکانیزم ضد دستکاری ساعت (Anti Clock-Tamper):**  
   اگر مشتری سعی کند ساعت سیستم یا ویندوز خود را به عقب بکشد تا زمان بیشتری بگیرد، سامانه متوجه ناهمخوانی زمانی شده و بلافاصله قفل و منقضی می‌شود.

۴. **مبهم‌سازی ۳ لایه (۳-Layer Obfuscation):**  
   کد نسخه نهایی با دستور زیر کامپایل و مبهم‌سازی می‌شود:
   ```bash
   npm run build:demo
   ```
   تمام نام‌ها، توابع، کلیدهای حافظه با XOR و مکانیزم Self-check پنهان می‌شوند تا هیچ امکانی برای دستکاری اسکریپت وجود نداشته باشد.

---

## 📞 تغییر شماره تماس در صفحه قفل

در فایل [`index.html`](./index.html) خط ۵۷۳ را پیدا کنید و شماره خود را جایگزین کنید:
```html
<a href="tel:09xxxxxxxxx" ...>
```
